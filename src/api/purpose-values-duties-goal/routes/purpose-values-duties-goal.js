'use strict';

/**
 * purpose-values-duties-goal router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::purpose-values-duties-goal.purpose-values-duties-goal');
