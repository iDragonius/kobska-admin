'use strict';

/**
 * purpose-values-duties-goal controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::purpose-values-duties-goal.purpose-values-duties-goal');
